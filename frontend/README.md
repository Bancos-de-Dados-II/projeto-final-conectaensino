# Conecta Ensino — Front-end Sprint 01

## Executar

```bash
cd frontend
cp .env.example .env
npm install
npm run dev
```

Abra o endereço exibido pelo Vite, normalmente `http://localhost:5173`.

O backend deve estar ativo em `http://localhost:3000`.
